Так, ну для начала про импорт пакетов - они все в начале только потому что так красивее выглядит
С базовыми модулями всё понятно, так что перейду к своим

Ну, my_module.py просто файл с одной функцией, я импортировал его и потом обращался к единственной функции
вот так my_module.prediction()

С успешным(Success) пакетом немного сложнее. В нем есть файл инит, который автоматически
запускается при импорте этого пакета, и в этом файле импортируются все 3 модуля, + еще сообщение
об успешном импорте пакета выводится в консоль. Ну вроде все